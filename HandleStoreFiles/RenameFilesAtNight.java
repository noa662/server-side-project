package HandleStoreFiles;

import java.io.File;

public class RenameFilesAtNight extends Thread{

    public void RenameFiles(String dir,String text){
        File file=new File(dir);
        for(File f:file.listFiles()){
            
        }
    }
}
